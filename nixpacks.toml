[phases.setup]
nixPkgs = ["jdk17", "maven"]

[phases.build]
cmds = ["cd backend && mvn -DskipTests clean package && cp target/meliani-trans-backend-1.0.0.jar ../app.jar"]

[start]
cmd = "java -jar app.jar"
